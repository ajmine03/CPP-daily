#include <stdio.h>

int main() {
    // int N;
    // scanf("%d",&N);
    int Standerd = 2000 ;
    int DULL = 4000 ;
    int Premium = 5000 ;

    int Person , Child , Day ;
    char RoomType ;
    printf("Input Person , RoomType(S/D/P) , child , day in order");
    scanf("%d %c %d %d",&Person,&RoomType,&Child,&day);

    //formula for minimum bill,
    if (Person == Child)
    {
        if (Person%2==0 && Child%2==0)
        {
            int room = Person/2 + Child/2
        }
        else if (Person%2==0 && Child%2==0)
        {
            int room = Person/2 + Child/2
        }
        
    }
    
    



    return 0;
}