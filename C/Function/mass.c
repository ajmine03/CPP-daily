#include <stdio.h>

int main()
{
    int N;
    scanf("%d", &N);

    while (N--)
    {
        int X, Y, Z;
        scanf("%d %d %d", &X, &Y, &Z);

        int S = 10 * X;
        
        int amount = S * Z;
        if(Y>X){
            
        }



        printf("%d\n", amount);
    }

    return 0;
}
