#include <stdio.h>
#include <math.h>
#include<string.h>

int main()
{
    chat S1[100005];
    chat S2[100005];
    int L1 = strlen(S2);
    int ar[L1];
    for (int i = 0; i < L1; i++)
    {
        scanf("%c",&S1)
        
    }

    fget(S1,sizeof(S1),stdin);
    int L = strlen(S2);
    for(int i = 0;i<L;i++){
        strcmp(S1,S2,)
    }
}